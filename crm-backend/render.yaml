services:
  - type: web
    name: 4biz_lead_crm_backend_python
    runtime: python
    region: frankfurt
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT